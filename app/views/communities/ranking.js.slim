- if @learn
  | $('#ranking-table').html('#{escape_javascript(render 'ranking_table', users: @learn_users, period: @learn_period, type: 'learn')}');
- else
  | $('#favo-ranking-table').html('#{escape_javascript(render 'ranking_table', users: @favorite_users, period: @favorite_period, type: 'favorite')}');
